((app) => {
    const config = {
        pixelSize: 8,
        circleCursorRadius: 50,
        squareCursorSize: 100,
		showHelper: false,
		tool: '8bit',
		cursorType: 'circle'
    };
    
    app.config = config;
})(app);